import type { ResponseMeta } from '@/types/envelope';

/** Phase 6 — scaffold (Section 7). Shows source / latency / confidence. */
export function MetaFooterBadge(_props: {
	meta?: ResponseMeta | null;
	confidence?: number | null;
}) {
	return null;
}
