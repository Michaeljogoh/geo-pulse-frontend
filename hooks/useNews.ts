'use client';

/** Phase 3 — scaffold (Section 7). */
export function useNews(_params: {
	country?: string;
	symbols?: string;
	lang?: string;
}) {
	throw new Error('useNews is not implemented yet (Phase 3)');
}
